package main
import "fmt"
func main() {

    var num1 int
    fmt.Scanln(&num1)

    /*vet := make([]int, 0, num1)

    for i := 0; i < num1; i++ {
        fmt.Scan(vet[i])
    }

    fmt.Println(vet) */
    fmt.Println(num1)
}
